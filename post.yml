apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgresql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgresql
  template:
    metadata:
      labels:
        app: postgresql
    spec:
      containers:
      - name: postgresql
        image: shammu101/shammudb
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          value: shammudb
        - name: POSTGRES_USER
          value: shammu
        - name: POSTGRES_PASSWORD
          value: shammu
